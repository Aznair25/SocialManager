# Copy to .env and fill in. Never commit .env (gitignored).

# OpenAI API key — used by src/generate.py and the web app (content generation
# only; rendering is fully local and needs no key).
OPENAI_API_KEY=

# Model for deck copy generation (optional, default gpt-5.1)
ZYLO_MODEL=gpt-5.1
